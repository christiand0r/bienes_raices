extends ../layouts/app


block styles


block content
  section(class="p-5 py-8")
      h2(class="text-center text-4xl font-extrabold")= page

      if properties.length > 0
        div(class="grid grid-cols-1 gap-4 mt-8 md:grid-cols-2 lg:grid-cols-4")
          each property in properties
            include ../includes/property-card
      else 
        p(class="text-center text-sm mt-8 text-slate-800") No hay propiedades registradas en esta categoría

block scripts
  
